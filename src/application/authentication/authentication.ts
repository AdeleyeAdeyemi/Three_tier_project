let req;

// The data structure that will save the updates from the web shell
req = {
   updateMessage: { timestamp, title },
   errors: Array,
    messages: Array,
}

export function my-platform-container() {
    return new HTMLContainer(
        "my-platform",
        class="container mb-4"
    )

}
